# API Configuration
server:
  host: 0.0.0.0
  port: 8000
  root_path: /api
  debug: true
  workers: 4

cors:
  origins:
    - "*"
  allow_methods:
    - "*"
  allow_headers:
    - "*"

logging:
  level: INFO
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
